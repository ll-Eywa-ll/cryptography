#include <iostream>
#include <graphics.h>
#include "welcomeScreen.h"
#include "mouse.h"

/*void mouseLeftClick(){

    cursorPosition();
    while(1) {
    //int leftClick = GetAsyncKeyState(VK_LBUTTON);

    if (GetAsyncKeyState(VK_LBUTTON)) {
        cleardevice();
        outtextxy(100,100, "hello");
    }
    }

}

void mouseRightClick() {
    cursorPosition();
    int rightClick = GetAsyncKeyState(VK_RBUTTON);
}
*/
